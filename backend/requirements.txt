# MediHelp Backend Dependencies v2.0
# Core
Flask==2.3.3
Flask-Cors==4.0.0
Flask-JWT-Extended==4.6.0
Werkzeug==2.3.7
python-dotenv==1.0.0
gunicorn==21.2.0

# AI / ML (required for ML engine)
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# NLP (required for spaCy preprocessing)
# After install: python -m spacy download en_core_web_sm
spacy>=3.6.0

# Email notifications (optional)
Flask-Mail>=0.9.1

# Rate limiting (optional)
Flask-Limiter>=3.5.0

# Background tasks / scheduled reminders (optional)
APScheduler>=3.10.0